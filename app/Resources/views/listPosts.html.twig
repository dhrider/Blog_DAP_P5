{% extends 'base.html.twig' %}

{% block pageContent %}
    <h1>Liste des Posts</h1>
    {% for post in posts %}
        <article>
            <h4>Publié le : <span>{{ post.dateLastModification | date('d-m-Y') }}</span></h4>
            <h3>{{ post.title }}</h3>
            <div>
                <p>{{ post.chapo }}</p>
            </div>
            <a href="#" style="color: white;">Lire le post</a>
            <br/>
        </article>
    {% endfor %}
{% endblock %}